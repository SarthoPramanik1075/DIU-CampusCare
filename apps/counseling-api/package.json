{
  "name": "@campuscare/counseling-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Counseling Service — deployable 2. Holds credential set B. No credential to campuscare_core exists (ADR-001). Refuses to start without [R3] crisis-protocol content (BR-68, EC-48).",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "dev": "tsx watch src/bootstrap/main.ts",
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/bootstrap/main.js"
  },
  "dependencies": {
    "@campuscare/shared-types": "workspace:*",
    "@fastify/cors": "^10.0.2",
    "dotenv": "^17.4.2",
    "fastify": "^5.2.1",
    "fastify-plugin": "^5.0.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.6",
    "typescript": "^5.7.3"
  }
}
